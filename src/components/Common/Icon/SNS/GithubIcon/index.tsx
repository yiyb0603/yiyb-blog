import { IconBaseProps } from 'react-icons';
import { AiOutlineGithub } from 'react-icons/ai';

const GithubIcon = (props: IconBaseProps): JSX.Element => {
  return (
    <AiOutlineGithub
      {...props}
    />
  );
}

export default GithubIcon;