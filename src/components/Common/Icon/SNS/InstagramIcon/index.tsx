import { IconBaseProps } from 'react-icons';
import { AiOutlineInstagram } from 'react-icons/ai';

const InstagramIcon = (props: IconBaseProps): JSX.Element => {
  return <AiOutlineInstagram {...props} />;
};

export default InstagramIcon;
