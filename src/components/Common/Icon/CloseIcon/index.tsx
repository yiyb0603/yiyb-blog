import { IconBaseProps } from 'react-icons';
import { VscClose } from 'react-icons/vsc';

const CloseIcon = (props: IconBaseProps): JSX.Element => {
  return <VscClose {...props} />;
};

export default CloseIcon;
