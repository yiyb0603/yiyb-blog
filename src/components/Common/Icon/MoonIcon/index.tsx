import { IconBaseProps } from 'react-icons';
import { BsMoon } from 'react-icons/bs';

const MoonIcon = (props: IconBaseProps): JSX.Element => {
  return (
    <BsMoon
      {...props}
    />
  );
}

export default MoonIcon;